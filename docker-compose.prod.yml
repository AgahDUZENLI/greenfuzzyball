services:

  backend:
    volumes: []
    command: sh -c "python3 db/migrate.py && uvicorn main:app --host 0.0.0.0 --port 8000"

  frontend:
    command: sh -c "npm install && npm run build && npm run preview -- --host --port 5173"